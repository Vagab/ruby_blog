<% if @articles.blank? %>
<p>
  No articles yet
</p>
<% else %>
<ul id = "articles">
 <% @articles.each do |article| %>
 <li><%= link_to article.title, {:action => 'show', :id => article.id} %></li>
 <% end %>
</ul>

<% end %>
